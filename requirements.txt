# Week 2 intentionally uses only the Python standard library.
# No chess engine dependency is required: engine.py is a self-contained
# Sunfish-equivalent implementation.
#
# Python >= 3.9 recommended.
#
# Optional extras a later week might add:
#   - pyyaml       (if best_config.yaml becomes non-trivial)
#   - matplotlib   (for plotting trends in Week 4)
